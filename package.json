{
  "name": "googlebooks-app",
  "version": "1.0.0",
  "description": "Using Google Books API & MERN stack, this app allows users to search for books and save & delete books from their reading list.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "Raimee Abbassi | Trilogy",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0"
  },
  "dependencies": {
    "apollo-server-express": "^2.15.1"
  }
}
